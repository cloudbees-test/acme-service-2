apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  workflow_dispatch:
jobs:
  deploy:
    uses: github.com/swashbuck1r/myapp-dec19/corp-workflows/service-deploy.yaml@main
